{
  "version": 2,
  "name": "bionathlon-api",
  "alias": ["bionathlon-api.now.sh"],
  "builds": [
    { "src": "index.js", "use": "@now/node-server" }
  ],
  "routes": [
    { "src": "^/api/(.*)", "dest": "/" }
  ],
  "env": {
    "DATABASE_URL": "@bionathlon_db"
  },
  "build": {
    "env": {
      "DATABASE_URL": "@bionathlon_db"
    }
  }
}
